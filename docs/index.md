---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "growthbook Provider"
description: |-
  Manage GrowthBook projects, environments, features and SDK connections through the GrowthBook REST API.
---

# growthbook Provider

Manage GrowthBook projects, environments, features and SDK connections through the GrowthBook REST API.

## Example Usage

```terraform
terraform {
  required_providers {
    growthbook = {
      source = "jz-wilson/growthbook"
    }
  }
}

provider "growthbook" {
  # Both can also come from GROWTHBOOK_API_KEY / GROWTHBOOK_API_URL.
  api_key = var.growthbook_api_key
  api_url = "https://growthbook.example.com/api"
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Optional

- `api_key` (String, Sensitive) GrowthBook secret API key (`secret_...`). Can also be set with the `GROWTHBOOK_API_KEY` environment variable.
- `api_url` (String) API root including the `/api` suffix, for example `https://growthbook.example.com/api`. Defaults to GrowthBook Cloud (`https://api.growthbook.io/api`). Can also be set with the `GROWTHBOOK_API_URL` environment variable.
