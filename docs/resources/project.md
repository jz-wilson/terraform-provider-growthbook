---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "growthbook_project Resource - growthbook"
subcategory: ""
description: |-
  Manages a GrowthBook project.
---

# growthbook_project (Resource)

Manages a GrowthBook project.

## Example Usage

```terraform
resource "growthbook_project" "example" {
  name            = "Example Project"
  description     = "Managed by Terraform"
  restrict_access = false

  settings = {
    stats_engine      = "bayesian"
    confidence_level  = 0.95
    p_value_threshold = 0.05
  }
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `name` (String) Display name of the project.

### Optional

- `description` (String) Description of the project.
- `public_id` (String) Stable identifier used in feature flag payloads. GrowthBook derives one from `name` when left unset.
- `restrict_access` (Boolean) Restrict this project to only members explicitly granted access.
- `settings` (Attributes) Per-project statistics settings overriding the organization defaults. Left unset, GrowthBook reports the organization defaults here once the project is created. (see [below for nested schema](#nestedatt--settings))

### Read-Only

- `date_created` (String) RFC3339 timestamp the project was created.
- `date_updated` (String) RFC3339 timestamp the project was last updated. Recomputed by GrowthBook on every update, so plans against this attribute are never guaranteed empty.
- `id` (String) GrowthBook project id (`prj_...`).

<a id="nestedatt--settings"></a>
### Nested Schema for `settings`

Optional:

- `confidence_level` (Number) Confidence level required to call a winner.
- `p_value_threshold` (Number) P-value threshold for statistical significance.
- `stats_engine` (String) Statistics engine, e.g. `bayesian` or `frequentist`.

## Import

Import is supported using the following syntax:

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
terraform import growthbook_project.example prj_abc123
```
